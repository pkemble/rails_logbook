<%= will_paginate @flights %>

<%= link_to 'view by entries', root_path %>
<table class="table table-striped table-condensed table-bordered">
	<tr>
		<th>date</th>
		<th>tail</th>
		<td>model</td>
		<td>dep</td>
		<td>arr</td>
		<td>PIC</td>
		<td>SIC</td>
		<td>block out</td>
		<td>block in</td>
		<td>night</td>
		<td>instrument</td>
		<td>approaches</td>
		<td>crew member</td>
	</tr>
	
	<% @flights.each do |f| %>
		<% @entry = f.entry %>
		<tr>
			<td><%= link_to @entry.date.strftime('%m-%d-%y'), edit_flight_path(f.entry_id, f.id) %></td>
			<td><%= @entry.tail %></td>
			<td><%= @entry.ac_model %></td>
			<td><%= f.dep %></td>
			<td><%= f.arr %></td>
			<td>
				<% if @entry.pic %>
					<%= f.block_time %>
				<% end %>
			</td>
			<td>
				<% if @entry.pic == false %>
					<%= f.block_time %>
				<% end %>
			</td>
			<td><%= f.blockout %></td>
			<td><%= f.blockin %></td>
			<td><%= f.night %></td>
			<td><%= f.instrument %></td>
			<td><%= f.approaches %></td>
			<td><%= @entry.crew_name %></td>
		</tr>
	<% end %>	
</table>

<%= will_paginate @flights %>
