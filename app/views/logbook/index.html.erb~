<p>
  Total flight hours in last 24 hours: <%= p 'todo' %><br>
  Total flight hours to date: <%= p @totals.time %><br>
  PIC: <%= p @totals.pic %><br>
  SIC: <%= p @totals.sic %><br>
  Instrument:<%= p @totals.instrument %><br>
  Night: <%= p @totals.night %>
  
</p>
<p>
<%= link_to 'new entry', new_entry_path, class: "btn btn-success" %>
</p>
<%= link_to 'view by flights', root_path(:view => 'flights') %>



<% unless params[:view] && params[:view] == 'flights' %>
	<%= render 'entries' %>
<% else %>
	<%= render 'flights' %>
<% end %>
